import { type MessagePort } from "node:worker_threads";
interface LoaderConfig {
    includes: string[];
    excludes: string[];
    coverage: boolean;
    seed?: number;
    port?: MessagePort;
}
export declare function initialize(data: LoaderConfig): void;
interface LoadResult {
    format?: string;
    source?: string | ArrayBuffer | SharedArrayBuffer | Uint8Array;
    shortCircuit?: boolean;
}
type LoadFn = (url: string, context: {
    format?: string | null;
}, nextLoad: (url: string, context: {
    format?: string | null;
}) => Promise<LoadResult>) => Promise<LoadResult>;
export declare const load: LoadFn;
export {};
