/**
 * Dictionaries can be used to provide additional mutation suggestions to the
 * fuzzer.
 */
export declare class Dictionary {
    private _entries;
    get entries(): string[];
    addEntries(dictionary: string[]): void;
}
export declare function addDictionary(...dictionary: string[]): void;
/**
 * Escapes a byte array to a string that can be used in a libFuzzer dictionary.
 * The format is a double-quoted string with escaped hex bytes.
 * @param byteArray
 * @returns The escaped string.
 *
 * Example:
 *    new Uint8Array([0,1,2,3]) will be converted to '"\\x00\\x01\\x02\\x03"'
 * Example:
 *    "Amazing" will be converted to '"\\x41\\x6d\\x61\\x7a\\x69\\x6e\\x67"'
 */
export declare function toEscapedString(byteArray: Uint8Array | Int8Array): string;
export declare function convertDictionaryEntry(entry: string | Uint8Array | Int8Array): string;
export declare function useDictionaryByParams(options: string[], additionalDictionaryEntries?: (string | Uint8Array | Int8Array)[]): string[];
