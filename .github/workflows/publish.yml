name: Publish

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      dist_tag:
        description: npm dist-tag
        required: true
        default: alpha
        type: choice
        options:
          - alpha
          - latest
      bump_version:
        description: bump package versions before publish
        required: true
        default: true
        type: boolean

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: npm

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: true

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: https://registry.npmjs.org

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.13
          no-cache: true

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Pre-check (workflow_dispatch only)
        if: github.event_name == 'workflow_dispatch'
        run: |
          mkdir -p packages/timezone/node_modules && ln -sfn "$PWD" packages/timezone/node_modules/mmntjs
          bun run build
          bun run lint
          TZ=UTC bun run test:hard
          bun run knip
          bun run fallow

      - name: Build
        run: bun run build

      - name: Bump version
        if: github.event_name == 'workflow_dispatch' && inputs.bump_version
        run: |
          if [ "${{ inputs.dist_tag }}" = "alpha" ]; then
            npm version prerelease --preid=alpha --no-git-tag-version
          else
            npm version patch --no-git-tag-version
          fi

      - name: Resolve publish metadata
        id: meta
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            DIST_TAG="${{ inputs.dist_tag }}"
          elif [[ "$VERSION" == *-* ]]; then
            DIST_TAG="alpha"
          else
            DIST_TAG="latest"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "dist_tag=$DIST_TAG" >> "$GITHUB_OUTPUT"

      - name: Publish
        run: npm publish --access public --tag "${{ steps.meta.outputs.dist_tag }}"

      - name: Create GitHub Release
        if: github.event_name == 'push' && !contains(github.ref_name, '-')
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3.0.0
        with:
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          prerelease: false
