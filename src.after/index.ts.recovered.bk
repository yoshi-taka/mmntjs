import { Moment, MomentInput, MomentConfig, momentProperties, setUpdateOffsetCallback, setRelTimeRounding, getRelTimeRounding, setRelTimeThreshold, getRelTimeThreshold } from './moment'
import { Duration, DurationInput, isDuration as checkIsDuration } from './duration'
import { isMoment, isDate, isString, isArray, isObject, isUndefined, isNumber, isObjectEmpty, createDateSafe } from './utils'
import { normalizeUnits as normUnits, daysInMonth, isLeapYear } from './units'
import { getLocale, setLocale, getCurrentLocale, defineLocale, updateLocale, getMonths, getWeekdays, Locale, listLocales, hasLocale, localeHasMissingParent } from './locale'

import type { LocaleSpec } from './locale/en'
import { parseString, parseArray, parseObject, parseTwoDigitYearFn as parseTwoDigitYearInternal, setParseTwoDigitYear } from './parse'
import { checkOverflow } from './moment'

export type { MomentInput } from './moment'
export type { DurationInput } from './duration'
export type { LocaleSpec } from './locale/en'

interface MomentStatic {
  (input?: MomentInput, format?: string | string[], strict?: boolean): Moment
  (input: string, format: string | string[], locale?: string, strict?: boolean): Moment

  duration(input?: DurationInput | number | string | Duration, unit?: string): Duration
  fn: typeof Moment.prototype
  version: string
  HTML5_FMT: Record<string, string>
  ISO_8601: string
  RFC_2822: string
  parseTwoDigitYear: (str: string) => number
  now: () => number
  utc(input?: MomentInput, format?: string | string[], strict?: boolean): Moment
  parseZone(input?: MomentInput, format?: string | string[], strict?: boolean): Moment
  unix(timestamp: number): Moment
  invalid(input?: MomentInput): Moment
  isMoment(obj: any): boolean
  isDate(obj: any): boolean
  isDuration(obj: any): boolean
  normalizeUnits(unit: string): string | undefined
  locale(locale?: string, ...args: any[]): string | Locale
  localeData(locale?: string): Locale
  defineLocale(locale: string, config: LocaleSpec): Locale
  updateLocale(locale: string, config: Partial<LocaleSpec>): Locale
  relativeTimeRounding(fn?: Function): Function | boolean
  relativeTimeThreshold(threshold: string, limit?: number): number | boolean
  min(...moments: MomentInput[]): Moment
  max(...moments: MomentInput[]): Moment
  months(format?: string, index?: number): string | string[]
  monthsShort(format?: string, index?: number): string | string[]
  weekdays(format?: string, index?: number): string | string[]
  weekdaysShort(format?: string, index?: number): string | string[]
  weekdaysMin(format?: string, index?: number): string | string[]
  suppressDeprecationWarnings: boolean
  deprecationHandler: ((name: string, msg: string) => void) | null
}

function hasAnyValue(parsed: any): boolean {
  return parsed.year !== undefined || parsed.month !== undefined || parsed.day !== undefined ||
    parsed.hour !== undefined || parsed.minute !== undefined || parsed.second !== undefined ||
    parsed.millisecond !== undefined || parsed.isoWeek !== undefined || parsed.isoWeekYear !== undefined ||
    parsed.dayOfYear !== undefined || parsed.quarter !== undefined ||
    parsed._week !== undefined || parsed._weekYear !== undefined
}

function scoreParsedResult(parsed: any): number {
  let score = 0
  if (parsed.year !== undefined) score += 10
  if (parsed.month !== undefined) score += 10
  if (parsed.day !== undefined) score += 10
  if (parsed.hour !== undefined) score += 3
  if (parsed.minute !== undefined) score += 2
  if (parsed.second !== undefined) score += 1
  if (parsed.millisecond !== undefined) score += 1
  return score
}

function moment(input?: MomentInput, format?: string | string[] | boolean, localeOrStrict?: string | boolean, fourthArg?: boolean): Moment {
  if (input === null) {
    return new Moment({
      _d: new Date(NaN),
      _i: input,
      _isValid: false,
      _nullInput: true,
      _overflow: -1,
    })
  }

  if (input === undefined) {
    if (format !== undefined && typeof format !== 'boolean' && !(isArray(format) && (format as any[]).length === 0)) {
      return new Moment({ _d: new Date(NaN), _i: input, _f: format as string | string[], _isValid: false, _nullInput: true })
    }
    const now = momentNowFn ? new Date(momentNowFn()) : new Date()
    return new Moment({ _d: now, _i: input })
  }

  if (isArray(input) && (input as any[]).length === 0 && format === undefined) {
    return new Moment({ _d: new Date(), _i: input })
  }
  if (isObject(input) && isObjectEmpty(input) && format === undefined) {
    return new Moment({ _d: new Date(), _i: input })
  }

  if (isMoment(input)) {
    return (input as Moment).clone()
  }

  if (isObject(input) && (input as any)._isAMomentObject) {
    const obj = input as any
    const cfg: MomentConfig = {
      _d: obj._d ? new Date(obj._d.getTime()) : new Date(NaN),
      _i: obj._i !== undefined ? obj._i : input,
      _f: obj._f,
      _l: obj._l,
      _isValid: obj._isValid !== undefined ? obj._isValid : true,
      _isUTC: obj._isUTC || false,
      _offset: obj._offset !== undefined ? obj._offset : 0,
      _strict: obj._strict || false,
      _overflow: obj._overflow !== undefined ? obj._overflow : -1,
      _parsedDateParts: obj._parsedDateParts || [],
    }
    if (obj._unusedTokens) cfg._unusedTokens = obj._unusedTokens
    if (obj._unusedInput) cfg._unusedInput = obj._unusedInput
    if (obj._charsLeftOver !== undefined) cfg._charsLeftOver = obj._charsLeftOver
    if (obj._empty !== undefined) cfg._empty = obj._empty
    if (obj._nullInput !== undefined) cfg._nullInput = obj._nullInput
    if (obj._invalidMonth !== undefined) cfg._invalidMonth = obj._invalidMonth
    if (obj._meridiem !== undefined) cfg._meridiem = obj._meridiem
    if (obj._iso !== undefined) cfg._iso = obj._iso
    if (obj._rfc2822 !== undefined) cfg._rfc2822 = obj._rfc2822
    if (obj._weekdayMismatch !== undefined) cfg._weekdayMismatch = obj._weekdayMismatch
    return new Moment(cfg)
  }

  if (isDate(input)) {
    const d = input as Date
    return new Moment({ _d: new Date(d.getTime()), _i: input })
  }

  if (isNumber(input)) {
    const n = input as number
    if (isNaN(n) || !isFinite(n)) {
      return new Moment({ _d: new Date(NaN), _isValid: false, _i: input })
    }
    if (format === 'X') {
      return new Moment({ _d: new Date(n * 1000), _i: input, _f: 'X' })
    }
    if (format === 'x') {
      return new Moment({ _d: new Date(n), _i: input, _f: 'x' })
    }
    if (format !== undefined) {
      return new Moment({ _d: new Date(NaN), _isValid: false, _overflow: -1, _i: input, _f: format as string })
    }
    return new Moment({ _d: new Date(n), _i: input })
  }

  if (isString(input)) {
    const str = input as string
    let strict = false
    let locale: string | undefined
    let fmt: string | string[] | undefined

    if (typeof format === 'boolean') {
      strict = format
    } else if (typeof localeOrStrict === 'boolean') {
      fmt = format as string | string[]
      strict = localeOrStrict
    } else {
      fmt = format as string | string[] | undefined
      if (typeof localeOrStrict === 'string') {
        locale = localeOrStrict
        if (locale && localeHasMissingParent(locale)) {
          locale = 'en'
        }
      }
      if (typeof fourthArg === 'boolean') {
        strict = fourthArg
      }
    }

    if (fmt) {
      if (isArray(fmt)) {
        const formats = fmt as string[]
        let bestParsed: any = null
        let bestScore = -99999
        let bestFormat: string | undefined

        for (const singleFmt of formats) {
          if (singleFmt === 'ISO_8601' || singleFmt === 'RFC_2822') {
            const parsed = parseString(str)
            if (parsed && hasAnyValue(parsed)) {
              const overflow = checkOverflow(parsed)
              let score = scoreParsedResult(parsed)
              score += 30
              if (parsed._empty === true) score -= 50
              if (overflow >= 0) score -= 100
              if (score > bestScore) {
                bestParsed = parsed
                bestScore = score
                bestFormat = singleFmt
              }
            }
            continue
          }
          const parsed = parseString(str, singleFmt, locale, strict)
          if (parsed) {
            const hasValue = hasAnyValue(parsed)
            const overflow = checkOverflow(parsed)
            let score = scoreParsedResult(parsed)
            score += 30
            if (overflow >= 0) score -= 100
            if (parsed._empty === true) score -= 50
            if (parsed._unusedTokens && parsed._unusedTokens.length > 0) score -= 10 * parsed._unusedTokens.length
            if (parsed._charsLeftOver > 0) score -= parsed._charsLeftOver * 3
            if (parsed._unusedInput) score -= parsed._unusedInput.reduce((a, s) => a + s.length, 0) * 2

            if (hasValue && score > bestScore) {
              bestParsed = parsed
              bestScore = score
              bestFormat = singleFmt
            }
          }
        }
            }
            continue
          }
          const parsed = parseString(str, singleFmt, locale, strict)
          if (parsed) {
            const hasValue = hasAnyValue(parsed)
            const overflow = checkOverflow(parsed)
            let score = scoreParsedResult(parsed)
            score += 30
            if (overflow >= 0) score -= 100
            if (parsed._empty === true) score -= 50
            if (parsed._unusedTokens && parsed._unusedTokens.length > 0) score -= 10 * parsed._unusedTokens.length
            if (parsed._charsLeftOver > 0) score -= parsed._charsLeftOver * 3
            if (parsed._unusedInput) score -= parsed._unusedInput.reduce((a: number, s: string) => a + s.length, 0) * 2

            if (hasValue && (score > bestScore || (score === bestScore && bestFormat && singleFmt.length < bestFormat.length))) {
              bestParsed = parsed
              bestScore = score
              bestFormat = singleFmt
            }
          }
        }

        if (bestParsed && hasAnyValue(bestParsed)) {
          const overflow = checkOverflow(bestParsed)
          if (overflow < 0) {
            const m = createMomentFromParsed(bestParsed, str, bestFormat as string, locale, strict)
            m._f = bestFormat
            m._parsedDateParts = bestParsed._parsedDateParts || []
            m._unusedTokens = bestParsed._unusedTokens || []
            m._unusedInput = bestParsed._unusedInput || []
            m._charsLeftOver = bestParsed._charsLeftOver || 0
            m._empty = bestParsed._empty !== false ? true : false
            m._invalidMonth = bestParsed._invalidMonth || null
            if (bestFormat === 'ISO_8601') m._iso = true
            return m
          }
          const config: MomentConfig = {
            _d: new Date(NaN),
            _i: str,
            _f: formats,
            _l: locale,
            _strict: strict,
            _isValid: false,
            _overflow: overflow,
          }
          if (bestParsed) {
            config._unusedTokens = bestParsed._unusedTokens
            config._unusedInput = bestParsed._unusedInput
            config._charsLeftOver = bestParsed._charsLeftOver
            config._empty = bestParsed._empty
            config._invalidMonth = bestParsed._invalidMonth
            config._weekdayMismatch = bestParsed._weekdayMismatch
            config._parsedDateParts = bestParsed._parsedDateParts
            config._meridiem = bestParsed._meridiem
          }
          return new Moment(config)
        }

        const emptyFormats = (fmt as string[]).length === 0
        const config: MomentConfig = {
          _d: new Date(NaN),
          _i: str,
          _f: fmt,
          _l: locale,
          _strict: strict,
          _isValid: false,
          _invalidFormat: emptyFormats,
        }
        if (bestParsed) {
          config._unusedTokens = bestParsed._unusedTokens
          config._unusedInput = bestParsed._unusedInput
          config._charsLeftOver = bestParsed._charsLeftOver
          config._empty = bestParsed._empty
          config._invalidMonth = bestParsed._invalidMonth
          config._weekdayMismatch = bestParsed._weekdayMismatch
          config._parsedDateParts = bestParsed._parsedDateParts
          config._meridiem = bestParsed._meridiem
        }
        return new Moment(config)
      }

      const fmtStr = fmt as string

      if (fmtStr === 'ISO_8601') {
        if (strict) {
          const hasExtendedDash = /^\d{4}-/.test(str.trim())
          const hasExtendedTime = /T\d{2}:\d{2}/.test(str)
          const hasBasicTimeMinutes = /T\d{4}/.test(str)
          if (hasExtendedDash && hasBasicTimeMinutes && !hasExtendedTime) {
            return new Moment({ _d: new Date(NaN), _i: str, _f: fmt as string, _l: locale, _strict: strict, _isValid: false, _iso: true })
          }
          if (!hasExtendedDash && hasExtendedTime) {
            return new Moment({ _d: new Date(NaN), _i: str, _f: fmt as string, _l: locale, _strict: strict, _isValid: false, _iso: true })
          }
          if (/W\d{2}[T ]\d/.test(str.trim()) && !/W\d{2}-?\d[T ]/.test(str.trim())) {
            return new Moment({ _d: new Date(NaN), _i: str, _f: fmt as string, _l: locale, _strict: strict, _isValid: false, _iso: true })
          }
        }
        const parsed = parseString(str)
        if (parsed && hasAnyValue(parsed)) {
          const overflow = checkOverflow(parsed)
          if (overflow >= 0) {
            return new Moment({ _d: new Date(NaN), _i: str, _f: fmt as string, _l: locale, _strict: strict, _isValid: false, _overflow: overflow, _iso: true })
          }
          if (strict) {
            if (parsed._hasDate && !parsed._hasTime && str.indexOf('T') >= 0) {
              return new Moment({ _d: new Date(NaN), _i: str, _f: fmt as string, _l: locale, _strict: strict, _isValid: false, _iso: true })
            }
            if (parsed._weekdayNum !== undefined && (parsed._weekdayNum === 0 || parsed._weekdayNum > 7)) {
              return new Moment({ _d: new Date(NaN), _i: str, _f: fmt as string, _l: locale, _strict: strict, _isValid: false, _overflow: 8, _iso: true })
            }
          }
          const m = createMomentFromParsed(parsed, str, fmt as string, locale, strict)
          m._iso = true
          return m
        }
        return new Moment({ _d: new Date(NaN), _i: str, _f: fmt as string, _l: locale, _strict: strict, _isValid: false, _iso: true })
      }

      if (fmtStr === 'RFC_2822') {
        const rfcParsed = parseString(str)
        if (rfcParsed && hasAnyValue(rfcParsed)) {
          let weekdayMismatch = false
          if (rfcParsed._weekdayName !== undefined && rfcParsed.day !== undefined) {
            const d = new Date(rfcParsed.year, rfcParsed.month, rfcParsed.day)
            const actualDay = d.getDay()
            if (rfcParsed._weekdayName !== actualDay) {
              weekdayMismatch = true
            }
          }
          const overflow = checkOverflow(rfcParsed)
          if (overflow >= 0) {
            const config: MomentConfig = {
              _d: new Date(NaN), _i: str, _f: fmt as string, _l: locale, _strict: strict,
              _isValid: false, _overflow: overflow, _rfc2822: false, _weekdayMismatch: weekdayMismatch,
            }
            return new Moment(config)
          }
          if (weekdayMismatch) {
            return new Moment({
              _d: new Date(NaN), _i: str, _f: fmt as string, _l: locale, _strict: strict,
              _isValid: false, _rfc2822: false, _weekdayMismatch: true,
            })
          }
          const m = createMomentFromParsed(rfcParsed, str, fmt as string, locale, strict)
          m._rfc2822 = true
          return m
        }
        return new Moment({ _d: new Date(NaN), _i: str, _f: fmt as string, _l: locale, _strict: strict, _isValid: false, _rfc2822: false })
      }

      let parsed = parseString(str, fmt as string, locale, strict)
      const config: MomentConfig = {
        _d: undefined as any,
        _i: str,
        _f: fmt as string,
        _l: locale,
        _strict: strict,
        _overflow: -1,
      }

      if (parsed) {
        if (parsed.bigHour) config._bigHour = true
        const hasValue = hasAnyValue(parsed)
        const overflow = checkOverflow(parsed)

        config._overflow = overflow
        config._unusedTokens = parsed._unusedTokens
        config._unusedInput = parsed._unusedInput
        config._charsLeftOver = parsed._charsLeftOver
        config._empty = parsed._empty
        config._invalidMonth = parsed._invalidMonth
        config._parsedDateParts = parsed._parsedDateParts
        config._meridiem = parsed._meridiem

        if (parsed.bigHour) config._bigHour = true

        let weekdayMismatch = false
        if (parsed._weekdayName !== undefined && parsed.year !== undefined && parsed.month !== undefined && parsed.day !== undefined) {
          const d = new Date(parsed.year, parsed.month, parsed.day)
          if (parsed._weekdayNum !== d.getDay()) {
            weekdayMismatch = true
          }
        }
        config._weekdayMismatch = weekdayMismatch

        if (overflow >= 0) {
          config._isValid = false
          config._d = new Date(NaN)
          return new Moment(config)
        }

        if (strict && parsed._unusedTokens && parsed._unusedTokens.length > 0) {
          config._isValid = false
          config._d = new Date(NaN)
          return new Moment(config)
        }

        if (strict && parsed._charsLeftOver > 0) {
          config._isValid = false
          config._d = new Date(NaN)
          return new Moment(config)
        }

        if (hasValue) {
          const m = createMomentFromParsed(parsed, str, fmt as string, locale, strict)
          if (weekdayMismatch) {
            m._isValid = false
            m._weekdayMismatch = true
          }
          return m
        }
      }

      config._isValid = false
      config._d = new Date(NaN)
      if (parsed) {
        config._unusedTokens = parsed._unusedTokens
        config._unusedInput = parsed._unusedInput
        config._charsLeftOver = parsed._charsLeftOver
        config._empty = parsed._empty
        config._invalidMonth = parsed._invalidMonth
        config._meridiem = parsed._meridiem
      }
      return new Moment(config)

    const trimmed = str.trim()
    if (trimmed === '') {
      return new Moment({ _d: new Date(NaN), _i: str, _isValid: false, _empty: true })
    }

    parsed = parseString(str)
    if (parsed) {
      const { year, month, day, hour, minute, second, millisecond, offset } = parsed
      const now = new Date()
      const y = year !== undefined ? year : now.getFullYear()
      const mo = month !== undefined ? month : now.getMonth()
      const d = day !== undefined ? day : now.getDate()
      const h = hour !== undefined ? hour : 0
      const min = minute !== undefined ? minute : 0
      const s = second !== undefined ? second : 0
      const ms = millisecond !== undefined ? millisecond : 0

      const parsedCheck = { year, month, day, hour, minute, second, millisecond }
      const overflow = checkOverflow(parsedCheck)

      let detectedFmt: string | undefined
      const trimmed = str.trim()
      const timeMatch = trimmed.match(/[T ](\d{2})(?::(\d{2})(?::(\d{2})(?:[.,](\d+))?)?)?/)
      const hasT = trimmed.indexOf('T') >= 0 || trimmed.indexOf('t') >= 0
      if (/^\d{4}-\d{2}-\d{2}([T ]|$)/.test(trimmed)) {
        detectedFmt = 'YYYY-MM-DD'
        if (timeMatch) {
          detectedFmt += (hasT ? 'T' : ' ') + 'HH'
          if (timeMatch[2] !== undefined) detectedFmt += ':mm'
          if (timeMatch[3] !== undefined) detectedFmt += ':ss'
          if (timeMatch[4] !== undefined) detectedFmt += '.SSSS'
        }
      } else if (/^\d{4}-\d{2}/.test(trimmed)) {
        detectedFmt = 'YYYY-MM'
      } else if (/^\d{4}/.test(trimmed)) {
        detectedFmt = 'YYYY'
      }

      let date: Date
      let config: MomentConfig = { _d: undefined as any, _i: str }
      if (detectedFmt) config._f = detectedFmt
      if (offset !== undefined) {
        date = createDateSafe(y, mo, d, h, min, s, ms, true)
        date = new Date(date.getTime() - offset * 60000)
        config._d = date
        if (overflow >= 0) {
          config._isValid = false
          config._overflow = overflow
        }
        return new Moment(config)
      }
      date = createDateSafe(y, mo, d, h, min, s, ms, false)
      config._d = date
      if (overflow >= 0) {
        config._isValid = false
        config._overflow = overflow
      }
      return new Moment(config)
    }

    const num = Number(str)
    if (!isNaN(num) && str.trim() !== '') {
      return new Moment({ _d: new Date(num), _i: str })
    }

    return new Moment({ _d: new Date(NaN), _i: str, _isValid: false })
  

  if (isArray(input)) {
    const arr = input as any[]
    if (arr.length === 0) {
      if (format === 'X' || format === 'x') {
        return new Moment({ _d: new Date(NaN), _i: arr, _isValid: false, _f: format as string })
      }
      return new Moment({ _d: new Date(), _i: arr })
    }
    const parsed = parseArray(arr)
    if (!parsed) {
      if (arr.some(v => v === null || v === undefined || (typeof v === 'number' && isNaN(v)))) {
        return new Moment({ _d: new Date(NaN), _i: arr, _isValid: false })
      }
      return new Moment({ _d: new Date(NaN), _i: arr, _isValid: false })
    }

    const overflow = checkOverflow(parsed)
    const d = createDateSafe(parsed.year, parsed.month, parsed.day, parsed.hour, parsed.minute, parsed.second, parsed.millisecond, false)
    if (overflow >= 0) {
      return new Moment({
        _d: d,
        _i: arr,
        _isValid: false,
        _overflow: overflow,
        _parsedDateParts: [parsed.year, parsed.month, parsed.day, parsed.hour, parsed.minute, parsed.second, parsed.millisecond],
      })
    }
    if (isNaN(d.getTime())) {
      return new Moment({ _d: new Date(NaN), _i: arr, _isValid: false })
    }
    return new Moment({ _d: d, _i: arr })
  }

  if (isObject(input)) {
    const obj = input as Record<string, any>
    const parsed = parseObject(obj)
    if (isObjectEmpty(parsed)) {
      return new Moment({ _d: new Date(), _i: obj })
    }
    const now = new Date()
    const year = parsed.year !== undefined ? parsed.year : now.getFullYear()
    const month = parsed.month !== undefined ? parsed.month : (parsed.year !== undefined ? 0 : now.getMonth())
    const day = parsed.day !== undefined ? parsed.day : (parsed.year !== undefined || parsed.month !== undefined ? 1 : now.getDate())
    const hour = parsed.hour !== undefined ? parsed.hour : 0
    const minute = parsed.minute !== undefined ? parsed.minute : 0
    const second = parsed.second !== undefined ? parsed.second : 0
    const ms = parsed.millisecond !== undefined ? parsed.millisecond : 0

    const overflow = checkOverflow({ year, month, day, hour, minute, second, millisecond: ms })
    const d = createDateSafe(year, month, day, hour, minute, second, ms, false)
    if (overflow >= 0) {
      return new Moment({
        _d: d,
        _i: obj,
        _isValid: false,
        _overflow: overflow,
      })
    }
    return new Moment({ _d: d, _i: obj })
  }

  return new Moment({ _d: new Date(NaN), _isValid: false, _i: input })


function weekYearToDate(isoWeekYear: number, isoWeek: number, isoWeekday: number): Date {
  const jan4 = new Date(0)
  jan4.setUTCFullYear(isoWeekYear, 0, 4)
  const dayOfJan4 = jan4.getUTCDay() || 7
  const mondayOfWeek1 = new Date(0)
  mondayOfWeek1.setUTCFullYear(isoWeekYear, 0, 4 - (dayOfJan4 - 1))
  const targetDate = new Date(mondayOfWeek1.getTime() + ((isoWeek - 1) * 7 + (isoWeekday - 1)) * 86400000)
  return targetDate
}

function localeWeekToDate(weekYear: number, week: number, weekday: number, dow: number, doy: number): Date {
  const jan1 = new Date(Date.UTC(weekYear, 0, 1))
  const fwd = 7 + dow - doy
  const fwdDate = new Date(Date.UTC(weekYear, 0, fwd))
  const fwdDay = fwdDate.getUTCDay()
  const fwdlw = (7 + fwdDay - dow) % 7
  const offset = -fwdlw + fwd - 1
  const week1Start = new Date(jan1.getTime() + offset * 86400000)
  const targetDate = new Date(week1Start.getTime() + ((week - 1) * 7 + weekday) * 86400000)
  return targetDate
}

function createMomentFromParsed(parsed: any, str: string, format?: string | string[], locale?: string, strict?: boolean): Moment {
  const baseConfig: Record<string, any> = (strict !== undefined) ? { _strict: strict } : {}
  if (parsed.bigHour) baseConfig._bigHour = true
  if (parsed._weekYear !== undefined && parsed._week !== undefined &&
      parsed.year === undefined && parsed.month === undefined && parsed.day === undefined) {
    const loc = getLocale(locale)
    const weekCfg = (loc._config as any).week || { dow: 0, doy: 6 }
    const weekdayRaw = parsed._localeWeekday !== undefined ? parsed._localeWeekday : (parsed._weekdayNum !== undefined ? parsed._weekdayNum : 0)
    const weekday = (weekdayRaw - weekCfg.dow + 7) % 7
    const d = localeWeekToDate(parsed._weekYear, parsed._week, weekday, weekCfg.dow, weekCfg.doy)
    if (parsed.hour !== undefined) d.setUTCHours(parsed.hour, parsed.minute || 0, parsed.second || 0, parsed.millisecond || 0)
    const config: MomentConfig = { _d: d, _i: str, _f: format, _l: locale, _parsedDateParts: parsed._parsedDateParts, ...baseConfig }
    if (parsed._unusedTokens) config._unusedTokens = parsed._unusedTokens
    if (parsed._unusedInput) config._unusedInput = parsed._unusedInput
    if (parsed._charsLeftOver !== undefined) config._charsLeftOver = parsed._charsLeftOver
    if (parsed._empty !== undefined) config._empty = parsed._empty
    if (parsed._invalidMonth !== undefined) config._invalidMonth = parsed._invalidMonth
    return new Moment(config)
  }

  if (parsed.isoWeekYear !== undefined && parsed.isoWeek !== undefined &&
      parsed.year === undefined && parsed.month === undefined && parsed.day === undefined) {
    const isoWeekday = parsed._weekdayNum !== undefined ? parsed._weekdayNum : 1
    const d = weekYearToDate(parsed.isoWeekYear, parsed.isoWeek, isoWeekday)
    if (parsed.hour !== undefined) d.setUTCHours(parsed.hour, parsed.minute || 0, parsed.second || 0, parsed.millisecond || 0)
    const config: MomentConfig = { _d: d, _i: str, _f: format, _l: locale, _parsedDateParts: parsed._parsedDateParts, ...baseConfig }
    if (parsed._unusedTokens) config._unusedTokens = parsed._unusedTokens
    if (parsed._unusedInput) config._unusedInput = parsed._unusedInput
    if (parsed._charsLeftOver !== undefined) config._charsLeftOver = parsed._charsLeftOver
    if (parsed._empty !== undefined) config._empty = parsed._empty
    if (parsed._invalidMonth !== undefined) config._invalidMonth = parsed._invalidMonth
    return new Moment(config)
  }

  if (parsed._weekYear !== undefined && parsed._week === undefined &&
      parsed.year === undefined && parsed.month === undefined && parsed.day === undefined) {
    const year = parsed._weekYear
    const d = new Date(year, 0, 1)
    const config: MomentConfig = { _d: d, _i: str, _f: format, _l: locale, _parsedDateParts: parsed._parsedDateParts, ...baseConfig }
    if (parsed._unusedTokens) config._unusedTokens = parsed._unusedTokens
    if (parsed._unusedInput) config._unusedInput = parsed._unusedInput
    if (parsed._charsLeftOver !== undefined) config._charsLeftOver = parsed._charsLeftOver
    if (parsed._empty !== undefined) config._empty = parsed._empty
    if (parsed._invalidMonth !== undefined) config._invalidMonth = parsed._invalidMonth
    return new Moment(config)
  }

  if (parsed.isoWeekYear !== undefined && parsed.isoWeek === undefined &&
      parsed.year === undefined && parsed.month === undefined && parsed.day === undefined) {
    const year = parsed.isoWeekYear
    const d = new Date(Date.UTC(year, 0, 1))
    const config: MomentConfig = { _d: d, _i: str, _f: format, _l: locale, _parsedDateParts: parsed._parsedDateParts, ...baseConfig }
    if (parsed._unusedTokens) config._unusedTokens = parsed._unusedTokens
    if (parsed._unusedInput) config._unusedInput = parsed._unusedInput
    if (parsed._charsLeftOver !== undefined) config._charsLeftOver = parsed._charsLeftOver
    if (parsed._empty !== undefined) config._empty = parsed._empty
    if (parsed._invalidMonth !== undefined) config._invalidMonth = parsed._invalidMonth
    return new Moment(config)
  }

  if (parsed.dayOfYear !== undefined && parsed.day === undefined && parsed.month === undefined) {
    const year = parsed.year !== undefined ? parsed.year : new Date().getFullYear()
    const maxDayOfYear = isLeapYear(year) ? 366 : 365
    if (parsed.dayOfYear > maxDayOfYear) {
      return new Moment({ _d: new Date(NaN), _i: str, _f: format, _l: locale, _isValid: false, _overflow: 2 })
    }
    const d = new Date(Date.UTC(year, 0, parsed.dayOfYear))
    const config: MomentConfig = { _d: d, _i: str, _f: format, _l: locale, _parsedDateParts: parsed._parsedDateParts, ...baseConfig }
    if (parsed._unusedTokens) config._unusedTokens = parsed._unusedTokens
    if (parsed._unusedInput) config._unusedInput = parsed._unusedInput
    if (parsed._charsLeftOver !== undefined) config._charsLeftOver = parsed._charsLeftOver
    return new Moment(config)
  }

  const isTimeOnly = parsed.hour !== undefined && parsed.year === undefined && parsed.month === undefined && parsed.day === undefined && parsed.offset === undefined

  if (isTimeOnly) {
    const d = new Date()
    d.setHours(parsed.hour || 0, parsed.minute || 0, parsed.second || 0, parsed.millisecond || 0)
    const config: MomentConfig = { _d: d, _i: str, _f: format, _l: locale, _parsedDateParts: parsed._parsedDateParts, _meridiem: parsed._meridiem, ...baseConfig }
    if (parsed._unusedTokens) config._unusedTokens = parsed._unusedTokens
    if (parsed._unusedInput) config._unusedInput = parsed._unusedInput
    if (parsed._charsLeftOver !== undefined) config._charsLeftOver = parsed._charsLeftOver
    if (parsed._empty !== undefined) config._empty = parsed._empty
    if (parsed._invalidMonth !== undefined) config._invalidMonth = parsed._invalidMonth
    return new Moment(config)
  }

  const hasYear = parsed.year !== undefined
  const hasMonth = parsed.month !== undefined
  const hasDay = parsed.day !== undefined

  let year = parsed.year !== undefined ? parsed.year : new Date().getFullYear()
  let month = parsed.month !== undefined ? parsed.month : (hasYear ? 0 : new Date().getMonth())
  const day = parsed.day !== undefined ? parsed.day : (hasYear || hasMonth ? 1 : new Date().getDate())
  let hour = parsed.hour !== undefined ? parsed.hour : 0
  const minute = parsed.minute !== undefined ? parsed.minute : 0
  const second = parsed.second !== undefined ? parsed.second : 0
  const ms = parsed.millisecond !== undefined ? parsed.millisecond : 0

  if (parsed.quarter !== undefined && month === 0 && !parsed.month) {
    month = (parsed.quarter - 1) * 3
  }

  let d: Date
  if (parsed.offset !== undefined) {
    d = createDateSafe(year, month, day, hour, minute, second, ms, true)
    d = new Date(d.getTime() - parsed.offset * 60000)
    const config: MomentConfig = {
      _d: d, _i: str, _f: format, _l: locale,
      _parsedDateParts: parsed._parsedDateParts, _meridiem: parsed._meridiem,
      ...baseConfig,
    }
    if (parsed._unusedTokens) config._unusedTokens = parsed._unusedTokens
    if (parsed._unusedInput) config._unusedInput = parsed._unusedInput
    if (parsed._charsLeftOver !== undefined) config._charsLeftOver = parsed._charsLeftOver
    return new Moment(config)
  }
  d = createDateSafe(year, month, day, hour, minute, second, ms, false)
  const config: MomentConfig = {
    _d: d, _i: str, _f: format, _l: locale,
    _parsedDateParts: parsed._parsedDateParts, _meridiem: parsed._meridiem,
    ...baseConfig,
  }
  if (parsed._unusedTokens) config._unusedTokens = parsed._unusedTokens
  if (parsed._unusedInput) config._unusedInput = parsed._unusedInput
  if (parsed._charsLeftOver !== undefined) config._charsLeftOver = parsed._charsLeftOver
  if (parsed._empty !== undefined) config._empty = parsed._empty
  if (parsed._invalidMonth !== undefined) config._invalidMonth = parsed._invalidMonth
  return new Moment(config)
}

const HTML5_FMT: Record<string, string> = {
  DATETIME_LOCAL: 'YYYY-MM-DDTHH:mm',
  DATETIME_LOCAL_SECONDS: 'YYYY-MM-DDTHH:mm:ss',
  DATETIME_LOCAL_MS: 'YYYY-MM-DDTHH:mm:ss.SSS',
  DATE: 'YYYY-MM-DD',
  TIME: 'HH:mm',
  TIME_SECONDS: 'HH:mm:ss',
  TIME_MS: 'HH:mm:ss.SSS',
  WEEK: 'GGGG-[W]WW',
  MONTH: 'YYYY-MM',
}

;(moment as any).duration = function(input?: DurationInput | number | string | Duration, unit?: string): Duration {
  return new Duration(input, unit)
}
;(moment as any).duration.invalid = function(): Duration {
  return Duration.invalid()
}
;(moment as any).duration.fn = Duration.prototype

;(moment as any).fn = Moment.prototype
;(moment as any).prototype = Moment.prototype
Object.defineProperty(Moment.prototype, 'clone', {
  value: Moment.prototype.clone,
  writable: true,
  configurable: true,
  enumerable: true,
})
;(moment as any).version = '2.30.1'
;(moment as any).momentProperties = momentProperties
;(moment as any).HTML5_FMT = HTML5_FMT
;(moment as any).ISO_8601 = 'ISO_8601'
;(moment as any).RFC_2822 = 'RFC_2822'
;(moment as any).parseTwoDigitYear = (function(str: string): number {
  const fn = parseTwoDigitYearInternal
  if (fn) return fn(str)
  const num = parseInt(str, 10)
  return num > 68 ? 1900 + num : 2000 + num
})
setParseTwoDigitYear(function(str: string): number {
  const num = parseInt(str, 10)
  return num > 68 ? 1900 + num : 2000 + num
})
Object.defineProperty(moment, 'parseTwoDigitYear', {
  get() { return (str: string) => {
    const fn = parseTwoDigitYearInternal
    if (fn) return fn(str)
    const num = parseInt(str, 10)
    return num > 68 ? 1900 + num : 2000 + num
  }},
  set(v: ((str: string) => number) | undefined) {
    setParseTwoDigitYear(v || undefined)
  },
  enumerable: true,
  configurable: true,
})
let momentNowFn: (() => number) | undefined
;(moment as any).now = function(): number {
  if (momentNowFn) return momentNowFn()
  return Date.now()
}
Object.defineProperty(moment, 'now', {
  get() { return momentNowFn || (() => Date.now()) },
  set(v: (() => number) | undefined) {
    momentNowFn = v || undefined
  },
  enumerable: true,
  configurable: true,
})
;(moment as any).isMoment = isMoment
;(moment as any).isDate = isDate
;(moment as any).isDuration = function(obj: any): boolean { return checkIsDuration(obj) }

;(moment as any).utc = function(input?: MomentInput, format?: string | string[], strict?: boolean): Moment {
  if (input === null) {
    return new Moment({ _d: new Date(NaN), _isValid: false, _isUTC: true, _offset: 0, _i: input, _nullInput: true })
  }
  if (input === undefined) {
    const now = momentNowFn ? new Date(momentNowFn()) : new Date()
    return new Moment({ _d: now, _isUTC: true, _offset: 0, _i: input })
  }
  if (isArray(input) && (input as any[]).length === 0) {
    return new Moment({ _d: new Date(), _isUTC: true, _offset: 0, _i: input })
  }
  if (isObject(input) && isObjectEmpty(input)) {
    return new Moment({ _d: new Date(), _isUTC: true, _offset: 0, _i: input })
  }
  if (isMoment(input)) {
    const m = (input as Moment).clone()
    m._isUTC = true
    m._offset = 0
    return m
  }
  if (isDate(input) || isNumber(input)) {
    const m = moment(input)
    m._isUTC = true
    m._offset = 0
    return m
  }
  if (isString(input)) {
    const str = input as string
    if (str === '') {
      return new Moment({ _d: new Date(NaN), _i: str, _isValid: false, _isUTC: true, _offset: 0, _empty: true })
    }
    if (format) {
      const m = moment(input, format as any, strict)
      m._isUTC = true
      m._offset = 0
      return m
    }
    const trimmed = str.trim()
    if (trimmed === '') {
      return new Moment({ _d: new Date(NaN), _i: str, _isValid: false, _isUTC: true, _offset: 0, _empty: true })
    }
    const parsed = parseString(str)
    if (parsed) {
      const { year, month, day, hour, minute, second, millisecond, offset } = parsed
      const parsedCheck = { year, month, day, hour, minute, second, millisecond }
      const overflow = checkOverflow(parsedCheck)
      const d = createDateSafe(year, month, day, hour, minute, second, millisecond, true)
      if (offset !== undefined) {
        const utcD = new Date(d.getTime() - offset * 60000)
        if (overflow >= 0) {
          return new Moment({ _d: utcD, _i: str, _isUTC: true, _offset: 0, _isValid: false, _overflow: overflow })
        }
        return new Moment({ _d: utcD, _i: str, _isUTC: true, _offset: 0 })
      }
      if (overflow >= 0) {
        return new Moment({ _d: d, _i: str, _isUTC: true, _offset: 0, _isValid: false, _overflow: overflow })
      }
      return new Moment({ _d: d, _i: str, _isUTC: true, _offset: 0 })
    }
    const num = Number(str)
    if (!isNaN(num) && str.trim() !== '') {
      const m = moment(num)
      m._isUTC = true
      m._offset = 0
      return m
    }
    return new Moment({ _d: new Date(NaN), _i: str, _isValid: false, _isUTC: true, _offset: 0 })
  }
  if (isArray(input)) {
    const arr = input as any[]
    if (arr.length === 0) return new Moment({ _d: new Date(), _isUTC: true, _offset: 0, _i: arr })
    const parsed = parseArray(arr)
    if (!parsed) {
      if (arr.some(v => v === null)) {
        return new Moment({ _d: new Date(NaN), _i: arr, _isValid: false, _isUTC: true, _offset: 0 })
      }
      return new Moment({ _d: new Date(), _i: arr, _isUTC: true, _offset: 0 })
    }
    const overflow = checkOverflow(parsed)
    const d = new Date(Date.UTC(parsed.year, parsed.month, parsed.day, parsed.hour, parsed.minute, parsed.second, parsed.millisecond))
    if (overflow >= 0) {
      return new Moment({
        _d: d,
        _i: arr,
        _isValid: false,
        _isUTC: true,
        _offset: 0,
        _overflow: overflow,
      })
    }
    if (isNaN(d.getTime())) {
      return new Moment({ _d: new Date(NaN), _i: arr, _isValid: false, _isUTC: true, _offset: 0 })
    }
    return new Moment({ _d: d, _i: arr, _isUTC: true, _offset: 0 })
  }
  if (isObject(input)) {
    const obj = input as Record<string, any>
    const parsed = parseObject(obj)
    if (isObjectEmpty(parsed)) {
      return new Moment({ _d: new Date(), _i: obj, _isUTC: true, _offset: 0 })
    }
    const now = new Date()
    const year = parsed.year !== undefined ? parsed.year : now.getUTCFullYear()
    const month = parsed.month !== undefined ? parsed.month : now.getUTCMonth()
    const day = parsed.day !== undefined ? parsed.day : now.getUTCDate()
    const hour = parsed.hour !== undefined ? parsed.hour : 0
    const minute = parsed.minute !== undefined ? parsed.minute : 0
    const second = parsed.second !== undefined ? parsed.second : 0
    const ms = parsed.millisecond !== undefined ? parsed.millisecond : 0
    const d = new Date(Date.UTC(year, month, day, hour, minute, second, ms))
    return new Moment({ _d: d, _i: obj, _isUTC: true, _offset: 0 })
  }
  return new Moment({ _d: new Date(NaN), _isValid: false, _isUTC: true, _offset: 0, _i: input })
}

;(moment as any).parseZone = function(input?: MomentInput, format?: string | string[], strict?: boolean): Moment {
  if (input === undefined) {
    return new Moment({ _d: new Date(NaN), _isValid: false, _isParseZone: true })
  }
  const m = moment(input, format as any, strict)
  m._isParseZone = true
  if (format && isString(input)) {
    const parsed = parseString(input as string, format as string, undefined, typeof strict === 'boolean' ? strict : false)
    if (parsed && parsed.offset !== undefined) {
      m._d = new Date(m._d.getTime() + parsed.offset * 60000)
      m._offset = parsed.offset
      m._isUTC = true
    }
  } else if (isString(input)) {
    const parsed = parseString(input as string)
    if (parsed && parsed.offset !== undefined) {
      m._d = new Date(m._d.getTime() + parsed.offset * 60000)
      m._offset = parsed.offset
      m._isUTC = true
    } else {
      m._offset = 0
      m._isUTC = true
    }
  }
  return m
}

;(moment as any).unix = function(timestamp: number): Moment {
  return moment(timestamp * 1000)
}

;(moment as any).invalid = function(input?: MomentInput): Moment {
  const config: any = { _d: new Date(NaN), _isValid: false, _userInvalidated: true }
  if (typeof input === 'object' && input !== null && !isArray(input) && !isMoment(input) && !isDate(input)) {
    for (const key of Object.keys(input)) {
      config['_' + key] = (input as any)[key]
    }
    delete config._userInvalidated
    config._i = input
  } else {
    config._i = input
  }
  return new Moment(config as MomentConfig)
}

;(moment as any).normalizeUnits = normUnits

;(moment as any).locale = function(locale?: string | string[], ...args: any[]): string | Locale {
  if (locale === undefined) {
    return getCurrentLocale()
  }
  if (Array.isArray(locale)) {
    return trySetLocaleWithFallbackArray(locale as string[]) || getCurrentLocale()
  }
  if (args.length > 0 && typeof locale === 'string') {
    if (hasLocale(locale.toLowerCase().replace(/_/g, '-'))) {
      updateLocale(locale, args[0] as LocaleSpec) as any
    } else {
      defineLocale(locale, args[0] as LocaleSpec) as any
    }
    return locale
  }
  if (typeof locale === 'string') {
    const result = trySetLocaleWithFallback(locale)
    if (result) return result
    return getCurrentLocale()
  }
  return getCurrentLocale()
}

function commonPrefixLen(a: string[], b: string[]): number {
  let i = 0
  while (i < a.length && i < b.length && a[i] === b[i]) i++
  return i
}

function trySetLocaleWithFallback(locale: string): string | undefined {
  const normalized = locale.toLowerCase().replace(/_/g, '-')
  const parts = normalized.split('-')
  for (let j = parts.length; j > 0; j--) {
    const candidate = parts.slice(0, j).join('-')
    if (hasLocale(candidate)) {
      setLocale(candidate)
      return candidate
    }
  }
  return undefined
}

function trySetLocaleWithFallbackArray(locales: string[]): string | undefined {
  for (let i = 0; i < locales.length; i++) {
    const normalized = locales[i].toLowerCase().replace(/_/g, '-')
    const parts = normalized.split('-')
    for (let j = parts.length; j > 0; j--) {
      const candidate = parts.slice(0, j).join('-')
      if (hasLocale(candidate)) {
        setLocale(candidate)
        return candidate
      }
      if (i + 1 < locales.length) {
        const nextNorm = locales[i + 1].toLowerCase().replace(/_/g, '-')
        const nextParts = nextNorm.split('-')
        if (nextParts.length >= j && commonPrefixLen(parts, nextParts) >= j - 1) {
          break
        }
      }
    }
  }
  return undefined
}

;(moment as any).localeData = function(locale?: string | Moment): Locale {
  if (locale && typeof locale === 'object' && (locale as any)._isAMomentObject) {
    return getLocale((locale as any)._l)
  }
  return getLocale(typeof locale === 'string' ? locale : undefined)
}

;(moment as any).lang = function(locale?: string | string[], ...args: any[]): string | Locale {
  if (locale === undefined) return getCurrentLocale()
  return (moment as any).locale(locale, ...args)
}

;(moment as any).langData = function(locale?: string): Locale {
  return (moment as any).localeData(locale)
}

;(moment as any).defineLocale = function(locale: string, config: LocaleSpec | null): Locale | void {
  if (config === null) {
    defineLocale(locale, null) as any
    return
  }
  const result = defineLocale(locale, config)
  if (result) return result
  setLocale('en')
  return getLocale('en')
}

;(moment as any).updateLocale = function(locale: string, config: Partial<LocaleSpec> | null): Locale {
  return updateLocale(locale, config as any)
}

;(moment as any).locales = function(): string[] {
  return listLocales()
}

;(moment as any).relativeTimeRounding = function(fn?: Function | boolean): Function | boolean {
  return setRelTimeRounding(fn as any)
}

;(moment as any).relativeTimeThreshold = function(threshold: string, limit?: number): number | boolean {
  return setRelTimeThreshold(threshold, limit) as any
}

;(moment as any).min = function(...args: MomentInput[]): Moment {
  if (args.length === 0) return moment() as any
  let inputList = args
  if (args.length === 1 && isArray(args[0]) && !isMoment(args[0])) {
    inputList = args[0] as any
  }
  let best: Moment | null = null
  let bestVal = Infinity
  let bestInvalid: Moment | null = null
  for (const item of inputList) {
    const m = isMoment(item) ? item as Moment : moment(item as any)
    const val = m.valueOf()
    if (isNaN(val) || !m.isValid()) {
      if (!bestInvalid) bestInvalid = m
    } else if (val < bestVal) {
      bestVal = val
      best = m
    }
  }
  return bestInvalid || best!
}

;(moment as any).max = function(...args: MomentInput[]): Moment {
  if (args.length === 0) return moment() as any
  let inputList = args
  if (args.length === 1 && isArray(args[0]) && !isMoment(args[0])) {
    inputList = args[0] as any
  }
  let best: Moment | null = null
  let bestVal = -Infinity
  let bestInvalid: Moment | null = null
  for (const item of inputList) {
    const m = isMoment(item) ? item as Moment : moment(item as any)
    const val = m.valueOf()
    if (isNaN(val) || !m.isValid()) {
      if (!bestInvalid) bestInvalid = m
    } else if (val > bestVal) {
      bestVal = val
      best = m
    }
  }
  return bestInvalid || best!
}

// Temporal bridge
;(moment as any).suppressDeprecationWarnings = false
;(moment as any).deprecationHandler = null as ((name: string, msg: string) => void) | null

Object.defineProperty(moment, 'calendarFormat', {
  get: function() { return (Moment as any).calendarFormat },
  set: function(v: any) { (Moment as any).calendarFormat = v },
  enumerable: true,
  configurable: true,
})

Object.defineProperty(moment, 'updateOffset', {
  get: function() { return (Moment as any).updateOffset },
  set: function(v: any) {
    (Moment as any).updateOffset = v
    const cb = typeof v === 'function' ? v : undefined
    setUpdateOffsetCallback(cb)
  },
  enumerable: true,
  configurable: true,
})

// Temporal bridge
import { toTemporal as toTemporalFn, fromTemporal as fromTemporalFn } from './temporal'

;(moment as any).fn.toTemporal = function() { return toTemporalFn(this) }
;(moment as any).fromTemporal = fromTemporalFn

;(moment as any).months = getMonths
;(moment as any).monthsShort = function(format?: string | number, index?: number): string | string[] {
  if (typeof format === 'number') return getMonths('short', format)
  if (format === undefined && index === undefined) return getMonths('short')
  return getMonths(format || 'short', index)
}
;(moment as any).weekdays = function(format?: string | number | boolean, index?: number): string | string[] {
  if (format === true) return getWeekdays('format', index as number | undefined)
  if (format === false) return getWeekdays(undefined, index)
  if (typeof format === 'number') return getWeekdays(undefined, format)
  return getWeekdays(format as string | undefined, index)
}
;(moment as any).weekdaysShort = function(format?: string | number | boolean, index?: number): string | string[] {
  if (typeof format === 'number') return getWeekdays('short', format)
  if (format === true) return getWeekdays('shortFormat', index as number | undefined)
  return getWeekdays('short', index)
}
;(moment as any).weekdaysMin = function(format?: string | number | boolean, index?: number): string | string[] {
  if (typeof format === 'number') return getWeekdays('min', format)
  if (format === true) return getWeekdays('minFormat', index as number | undefined)
  return getWeekdays('min', index)
}

// Migration tooling
import { configure, report as reportFn, Moment2Config, Moment2UsageReport } from './migration'
;(moment as any).config = configure
;(moment as any).report = reportFn

export { Moment, Duration, Locale, moment as default, moment, isMoment, isDate }
export type { MomentConfig }
